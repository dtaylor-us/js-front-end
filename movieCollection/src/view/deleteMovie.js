mc.view.deleteMovie = {
    setupUI: function () {
        // target delete button
        // target select box in form
        // init key(string) keys(list) movie(null) optionEl(null) i(0)
        // call retrieve objects from model
        // store list of keys from instances list into keys var
        // iterate over keys and set key to key index key
        // create option element for each element in list
        // set text(title) and value(movieID) attrib on option element
        // add event click listener to delete button and cal handleDeteEvent
        // add  befure unload event listener to window to saveAll models to storage
    },
    handleDeleteEvent: function () {
        // target selectEl on form
        // target select value
        // if value is present call delete method from model
        // remove selected item from select box
    }
}
